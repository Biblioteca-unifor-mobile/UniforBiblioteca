<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo — mais suave que #f0f6ff (usei #f7fbff) -->
    <solid android:color="#F7FBFF"/>

    <!-- Borda sutil -->
    <stroke
        android:width="1dp"
        android:color="#E6EEF8"/>

    <!-- Cantos arredondados -->
    <corners android:radius="12dp"/>

    <!-- Sombra interna/gradiente sutil opcional -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp"/>
</shape>
